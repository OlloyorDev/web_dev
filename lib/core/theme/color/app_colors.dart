import 'dart:ui';

class AppColors {
  // Light mode — warm Zinc palette
  static const lightBackground = Color(0xFFFAFAF9);
  static const lightCard = Color(0xFFFFFFFF);
  static const lightTextPrimary = Color(0xFF18181B);
  static const lightTextSecondary = Color(0xFF71717A);
  static const lightDivider = Color(0xFFE4E4E7);

  // Dark mode
  static const darkBackground = Color(0xFF0F172A);
  static const darkCard = Color(0xFF1E293B);
  static const darkTextPrimary = Color(0xFFE2E8F0);
  static const darkTextSecondary = Color(0xFF94A3B8);
  static const darkDivider = Color(0xFF334155);

  // Accent
  static const primaryLight = Color(0xFF2563EB);
  static const primaryDark = Color(0xFF60A5FA);
  static const secondaryLight = Color(0xFF7C3AED);
  static const secondaryDark = Color(0xFFA78BFA);
}
